{
  "name": "age-calculator",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy dist --project-name age-calculator",
    "cf:preview": "astro build && wrangler pages dev dist"
  },
  "dependencies": {
    "astro": "^6.4.4"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^13.6.1",
    "wrangler": "^4.98.0"
  }
}
